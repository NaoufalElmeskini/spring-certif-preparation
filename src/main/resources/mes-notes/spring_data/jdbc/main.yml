
jdbc:
  :
    Java DataBase Connectivity
    est une interface Java
    
    introduit des INTERFACES utiles pour manipuler efficacement la couche BDD
    
    necessite un DRIVER (mysql, mongodb...) qui implemente ces interfaces.

  concretement :
    package: javax.sql
    exemples:
      DataSource: objet contennt les parametres de connexiçon a la base
      Statement, PreparedStatement... : contient la requete literale a executer par la base
      ResultSet: represente le resultat d'une requete

ORM (Object Relation Mapping):
  est un framework qui fournit une technologie de mapping entre Relation (DB) et Objet (Java)
  souvent implemente le standard JPA
  ::> Hibernate, EclipseLink, ...
###########
spring-data-jdbc:
  :

  JdbcTemplate :
    utiliser le DesignPattern Template
    permet d'obfusquer le bruit d'ouverture/fermeture/try/catch



